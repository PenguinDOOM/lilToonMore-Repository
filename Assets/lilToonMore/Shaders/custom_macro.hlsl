//----------------------------------------------------------------------------------------------------------------------
// Macro

#define LIL_FEATURE_MAIN4_6TH // MUST BE ON
//#define LIL_FEATURE_MAIN7THORLATER // Optional

/*
// Why can't I use lilbool and TEXTURE2D(tex)?
#if !defined(lilBool)
    #define lilBool uint
#endif

#if defined(SHADER_API_D3D11)
    #if !defined(TEXTURE2D)
        #define TEXTURE2D(tex) Texture2D tex
    #endif
#endif
*/
